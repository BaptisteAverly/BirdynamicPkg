% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bdy_get_cost_raster.R
\name{bdy_get_cost_raster}
\alias{bdy_get_cost_raster}
\title{Get cost transition matrix}
\usage{
bdy_get_cost_raster(
  colonies,
  shapeBuffer = 100,
  pixel_size = 1000,
  N_buffer = 100,
  S_buffer = 100,
  W_buffer = 100,
  E_buffer = 100,
  returnRaster = F,
  plotOutput = F
)
}
\arguments{
\item{colonies}{vector of geometries, giving the coordinates of bird colonies, wind farms, or others.
This is used to get the extent of the raster to calculate.}

\item{pixel_size}{numeric, size in meter of the cells represented by one pixel of the raster. Lower numbers give higher resolution, but greatly increase computing time.}

\item{N_buffer}{north buffer to increase the extent of the raster beyond the northernmost colony or windfarm (numeric, in kilometers)}

\item{S_buffer}{south buffer to increase the extent of the raster beyond the southernmost colony or windfarm (numeric, in kilometers)}

\item{W_buffer}{west buffer to increase the extent of the raster beyond the westernmost colony or windfarm (numeric, in kilometers)}

\item{E_buffer}{east buffer to increase the extent of the raster beyond the easternmost colony or windfarm (numeric, in kilometers)}
}
\value{
List of 2 elements:
\itemize{
\item 'transition_matrix': transitionLayer object where sea is represented as low cost and land is represented as high cost
\item 'cost_raster': raster object where sea is represented as low cost and land is represented as high cost
}
}
\description{
Produces the transition matrix and cost raster used to calculate shortest path distances for birds with marine exclusive livestyles.
}
